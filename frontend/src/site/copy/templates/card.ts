import type { CopyMap } from '../types';

export const CARD_COPY = {
  'hero.kicker': 'Together with their families',
  'countdown.daysToGo': 'days to go',
  'rsvp.cta': 'RSVP',
  'final.website': 'View our website',
} as const satisfies CopyMap;
