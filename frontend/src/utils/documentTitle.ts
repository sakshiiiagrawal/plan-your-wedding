/**
 * The marketing title from index.html. Screens that retitle the document (for
 * page-specific print/PDF filenames) restore this on unmount, so it has to
 * match index.html exactly — keep the two in sync.
 */
export const DEFAULT_DOC_TITLE = 'shaadi.diy — Every detail of your wedding, one home';
